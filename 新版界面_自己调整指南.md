# 新版界面 · 自己调整速查

> 工程根：`D:\ZJFJ\计时器\保护板\行云保护板\协议\BMS上位机 行云`
> 解决方案：`BMS上位机V3.1.sln`
> 本文里给的行号可能随改动小幅移动，**找不到就用"搜索关键字"那一列去搜**。

---

## 一、我现在跑的是哪一份？

| | 路径 | 说明 |
|---|---|---|
| **新版 exe** | `BMS上位机\bin\Release\BMS上位机.exe` | VS 里"重新生成解决方案"会覆盖它。**双击这个就是新版** |
| 旧版 exe | `协议\BMS上位机 V3.1.zip`、`V2.6(1).7z`、`V3.2.7z` 里解出来的 | 那是**旧代码**，打开是"CELL 两列表格 + 两个大圆圈 MOS"的老界面 |
| 旧的 Debug 输出 | `BMS上位机\bin\Debug\...` | 很久没更新过，别用它判断新旧 |

**一眼分辨新旧**：

| | 旧版 | 新版 |
|---|---|---|
| 顶部页签 | 系统灰底方角 | **自绘**：选中项上方有 3px 蓝条 + 左侧小蓝点 |
| 电池信息页 | CELL 1~13 / 21~33 两列表格、两个大圆圈 MOS | **四列卡片**：电芯表 ｜ 电池状态（SOC 大字＋电池图形＋NTC 温度行＋放电/充电 MOS）｜ 光伏和市电状态（太阳能板/电塔形象图）｜ 保护标志 |
| 基本参数 / 校准控制 | 灰色 3D 框、绝对坐标 | **卡片化**：每张卡片左上角 3px 蓝条＋标题 |
| 右上角 | 无 | 「保存到文件」开关 ＋「网页互动」按钮 |

判断改动有没有生效的最快办法：**看 `bin\Release\BMS上位机.exe` 的时间戳**（重新生成后它一定会变）。

---

## 二、三个页面的代码在哪

| 页面 | 新版代码 | 回退后的老界面 |
|---|---|---|
| 电池信息 | `BMS上位机\BatteryPageNew.cs` | `Form1.Designer.cs`（绝对坐标那一套） |
| 基本参数 | `BMS上位机\ParamPagesNew.cs` | 同上 |
| 校准控制 | `BMS上位机\ParamPagesNew.cs` | 同上 |

> ⚠️ **不要用 VS 的「查看设计器」打开 Form1** —— `Form1.Designer.cs` 是按 150% 基准手工维护的，
> 设计器会按当前系统字体把所有坐标重算（`ClientSize 1852×1085 → 1069×587`），一次毁掉整个文件。
> 要改 Designer 直接改代码文本。

---

## 三、想改什么 → 改哪里

### 基本参数 / 校准控制 两页（`ParamPagesNew.cs` 顶部"统一规格"段，约 44~70 行）

| 想改 | 常量 | 现在 | 说明 |
|---|---|---|---|
| 正文/控件字号 | `PP_FONT_SIZE` | `9f` | 全页统一用这个字号 |
| 卡片标题字号 | `PP_TITLE_SIZE` | `9.5f` | |
| 卡片标题条高度 | `PP_TITLE_H` | `22` | |
| **上下行间距** | `PP_ROW_GAP` | `8f` | 行距 = 控件高 + 该值。**想紧凑就调小**（如 6） |
| **输入框圆角框大小** | `PP_FIELD_PAD` | `2f` | 框 = 控件矩形 ± 该值 |
| **框与框之间的缝** | —— | 4px | 缝 = `PP_ROW_GAP − 2×PP_FIELD_PAD`。调间距要同时看这两个 |
| 圆角半径 | `PP_FIELD_RADIUS` | `3f` | 想更圆就加大（如 5） |
| 整个框上下微调 | `PP_FIELD_NUDGE` | `0` | 正数 = 框往下。实机差一两像素就改它 |
| 圆角框开关 | `PP_ROUND_FIELDS` | `true` | 改 `false` = 回到系统原生方框（出问题时的退路） |
| 页面底色 | `PP_BG` | `240,242,245` | |
| 卡片底色 / 边线 | `PP_CARD` / `PP_EDGE` | `White` / `222,228,236` | |
| 卡片标题字色 / 强调色 | `PP_TITLE` / `PP_ACCENT` | `38,50,64` / `47,128,237` | |
| 输入框边线色 | `PP_FIELD_EDGE` | `184,196,210` | |
| 按钮边线色 | `PP_BTN_EDGE` | `199,210,222` | |
| **给某张卡片补一行说明文字** | `PP_HEADER_FIX`（约 261 行） | `{ "groupBox7", "模式" }` | 键 = 卡片控件名，值 = 那一行显示的文字。`"groupBox7"` 是校准页「开关控制」；想换词就改 `"模式"`，想给别的卡加就往字典里加一行 |

改字号后如果文字被裁，把 `FillCard()` 里的 `c.Font.Height + 10` 那个 **10** 调大（`PageInputH()` 里的 `+ 10f` 要一起改，两处必须一致）。

### 电池信息页（`BatteryPageNew.cs`）

| 想改 | 搜索关键字 | 说明 |
|---|---|---|
| 顶部页签宽/高 | `tabControl1.ItemSize = new Size(134, 36)` | 约 645 行 |
| 页签颜色（未选/文字/强调） | `Color.FromArgb(228, 231, 235)`、`(95, 94, 90)`、`(47, 128, 237)` | `StyleTabs()` 里，约 693~698 行 |
| 右上角两个控件尺寸 | `_bpSaveToggle.Size = new Size(158, 34)`、`m_btnOpenDemo.Size = new Size(146, 34)` | 约 780 / 800 行。**高度 34 是为了正好塞进 36px 的标签条** |
| 电芯表列宽（序号/电压/均衡） | `NewCol("序号"` / `NewCol("电压 (mV)"` / `NewCol("均衡"` | 改 `FillWeight` |
| 电池图形几格 | `const int CELLS = 5;` | 约 1728 行 |
| NTC 热力环的颜色范围 | `hue`（`NtcBadge` / `BatteryView` 内） | `t∈[-10,60] → hue=120*(1-x)`（绿→黄→红） |
| 后面那几列的字号 | `new Font("Microsoft YaHei", ...)` | 电池页字号是写死的（8.5F~15F），**要逐个改** |

---

## 四、改完怎么生效

1. **先在 VS 里关掉所有 `.cs` 文件**（否则 VS 可能拿旧的内存副本覆盖磁盘上的改动）；
2. VS 里点 **「重新生成解决方案」**（不是只按 F5 —— F5 可能跑旧 exe）；
3. 确认 `BMS上位机\bin\Release\BMS上位机.exe` 的**时间戳变了**；
4. 运行它。

判断"改动有没有进来"的小技巧：**行距**。
- `Font.Height(16) + 10 = 26` 是控件高 → 行距 = 26 + 8 = **34**
- 如果看到的行距还是 30/38，说明跑的不是最新版

---

## 五、出问题一键回退（改一个 `true`/`false` 就行）

| 位置 | 改成 | 效果 |
|---|---|---|
| `BatteryPageNew.cs:28` `USE_NEW_BATT_PAGE` | `false` | 电池信息页回到老的 CELL 两列表格界面 |
| `ParamPagesNew.cs:41` `USE_NEW_PARAM_PAGES` | `false` | 基本参数 / 校准控制 回到 Designer 老界面 |
| `ParamPagesNew.cs:793` `PP_ROUND_FIELDS` | `false` | 输入框/下拉框不用自绘圆角，改回系统原生方框 |

三个开关互相独立，出一个问题只关一个，其余不受影响。

---

## 六、这些别碰

| | 原因 |
|---|---|
| `Form1.Designer.cs` | 别用"查看设计器"打开（会把坐标全部重算）。要改就手写代码 |
| `Form1.cs` 里的 `Two_Bytes_Write` | **必须保留 `UInt16.Parse`**（会抛异常）—— 写下去的值必须是确认过的，不能兜底写 0 |
| `Form1.cs` 里的 `Write_McuE2_Data` 赋值段 | 整段共用一个 `try/catch`，任何一项非法就整包不写（这是刻意的） |
| `bin\Release\perf.log` | 性能探针：只在某一步 >120ms 时才会生成。界面卡的话把它发给我就能定位 |
